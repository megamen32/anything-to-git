# Demo grant portal adapter

Offline demonstration of a concrete-site adapter. It splits one grant
application into three pages, four semantic blocks, and four canonical files.

The website itself does not exist. The fixtures let the complete Git/merge/plan
flow run without browser access.
